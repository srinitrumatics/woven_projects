# Data Model: Scrollable Tab Header Rows on Order Details Page

**N/A** — this feature is a single Tailwind CSS className change on an existing page. It introduces no new data entities, no database schema changes, and no Salesforce field usage.

No component prop, state, or data-fetching interface changes are required; only the className string passed to one existing `<Tabs>` element in `app/orders/[id]/OrderClientPage.tsx` (see `research.md`).
